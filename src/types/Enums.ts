export enum OrderBy {
  ORDER_BY_UNSPECIFIED = 0,
  ORDER_BY_ASC = 1,
  ORDER_BY_DESC = 2,
  UNRECOGNIZED = -1,
}

export enum CollectionState {
  OPEN = 0,
  PAUSED = 1,
  CLOSED = 2,
  UNRECOGNIZED = -1,
}
